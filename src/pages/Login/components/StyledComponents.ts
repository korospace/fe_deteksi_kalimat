import { Box, styled } from "@mui/material";

export const ContainerLoginPage = styled(Box)(() => ({
    width: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
}));